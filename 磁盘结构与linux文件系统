磁盘结构与linux文件系统
	软盘
		面 0，1面 
		磁道 (Track) 磁针旋转一周的轨迹 最外侧是0磁道
		扇面区(Sector)  每个磁道又分成若干段 每一段叫做扇区 (512Byte)
		面，道号，扇面 =》扇区地址
		磁盘与主机交换是以扇面为单位的，例如，扇面上只有一个字节的数据，主机也是将整个扇面全部读取到内存，再取出数据。

		格式化

		FAT32

			链式存储

		lnux文件系统 ext2/3

			索引式存储